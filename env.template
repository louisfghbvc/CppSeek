# CppSeek - NVIDIA NIM API Configuration Template
# ===============================================
# 
# 複製此檔案為 .env 並填入你的配置
# Copy this file as .env and fill in your configuration

# NVIDIA NIM API 金鑰 (必需 / Required)
# 從 https://catalog.ngc.nvidia.com/ai-foundation-models 取得
# Get from https://catalog.ngc.nvidia.com/ai-foundation-models
NIM_API_KEY=your_nvidia_api_key_here

# NVIDIA NIM API 設定 (可選，使用預設值 / Optional, using defaults)
# ================================================================

# API 端點 URL / API Endpoint URL
# NIM_BASE_URL=https://integrate.api.nvidia.com/v1

# Embedding 模型名稱 / Embedding Model Name
# NIM_MODEL=nvidia/llama-3.2-nv-embedqa-1b-v2

# API 請求超時時間 (毫秒) / API Request Timeout (milliseconds)
# NIM_TIMEOUT=30000

# 失敗重試次數 / Retry Attempts on Failure
# NIM_RETRY_ATTEMPTS=3

# 最大並發請求數 / Maximum Concurrent Requests
# NIM_MAX_CONCURRENT_REQUESTS=10

# 批次處理大小 / Batch Processing Size
# NIM_BATCH_SIZE=50

# 進階設定 (通常不需要修改 / Advanced Settings - Usually no need to modify)
# ===========================================================================

# Chroma 向量資料庫 URL (如果使用本地 Chroma 服務)
# Chroma Vector Database URL (if using local Chroma service)
# CHROMA_URL=http://localhost:8000

# 是否啟用除錯模式 / Enable Debug Mode
# CPPSEEK_DEBUG=false

# 最大記憶體使用量 (MB) / Maximum Memory Usage (MB)
# CPPSEEK_MAX_MEMORY=200

# 配置優先級說明 / Configuration Priority:
# =====================================
# 1. VS Code 設定 (最高優先級 / Highest Priority)
# 2. 環境變數 (中等優先級 / Medium Priority)
# 3. .env 檔案 (最低優先級 / Lowest Priority)
#
# 推薦使用 VS Code 設定來儲存 API 金鑰以確保安全性
# Recommended to use VS Code settings to store API key for security

# 安全提醒 / Security Reminder:
# ============================
# 1. 不要將此檔案提交到版本控制 / Don't commit this file to version control
# 2. 將 .env 加入到 .gitignore / Add .env to .gitignore
# 3. 妥善保管你的 API 金鑰 / Keep your API key secure
# 4. 定期更換 API 金鑰 / Rotate API key regularly
